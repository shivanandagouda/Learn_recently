pipeline {
    agent any
        stages {
            stage (ansible play book run} 
                   {
                       steps { 
                           script {
                bat 'docker build -t .'
                           }
                       }
        }
    }
}
