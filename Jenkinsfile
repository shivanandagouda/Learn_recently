pipeline {
  agent any 
     stages
            {
           stage(first) 
              {
                steps {
                 script 
                {
            bat 'docker version'
                 }
            }
      }
}
}

